# workspace — rules

Confirmed conventions; apply by default when working in this repo.

- Prefer typed IPC via the preload `contextBridge` contract; the renderer does not use Node or SQLite directly.
