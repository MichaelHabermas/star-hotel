# Open-source contribution artifact (course submission)

The course spec asks for proof of **OSS contribution** (tooling, template, blog, or upstream fix). Use one row below and **paste the canonical URL** into [README.md](../README.md) when finalized.

| Option | Description | URL |
| ------ | ----------- | --- |
| A. Upstream PR | Bugfix or doc improvement to a dependency (e.g. electron-vite, better-sqlite3 docs) | _Add link_ |
| B. Reusable template | Public repo or gist with scaffold notes for Electron + SQLite + Zod | _Add link_ |
| C. Technical post | Public write-up of IPC + Express-in-main pattern or migration lessons | _Add link_ |

## This repository

Star Hotel itself may be public as the **course project** ([PUBLIC-GITHUB-CHECKLIST.md](./PUBLIC-GITHUB-CHECKLIST.md)). That satisfies visibility of your work; the **OSS contribution** row should still point to something **beyond** the assignment repo if the rubric expects a distinct contribution—confirm with your instructor.

**Default pointer (replace if you use A/B/C above):** repository root — `https://github.com/<org>/<repo>` _(update when published)_
